<project name="MavenDS" basedir=".">
	<description>MavenDS: A Dataset and Analysis Framework for the Maven Repository</description>
	<property name="src" location="src" />
	<property name="resources" location="resources" />
	<property name="lib" location="lib" />
	<property name="build" location="build" />
	<property name="cache" location="cache" />
	<property name="out" location="out" />
	<property name="mirror.1" value="http://mirrors.ibiblio.org/maven2" />
	<property name="mirror.2" value="http://maven.antelink.com/content/repositories/central" />
	<property name="mirror.3" value="http://repo.jfrog.org/artifactory/simple/libs-release-bintray" />
	<property name="nexusindexgz.url" value="${mirror.1}/.index/nexus-maven-repository-index.gz" />
	<property name="nexusindexgz.path" location="${cache}/nexus-maven-repository-index.gz" />
	<property name="nexusindex.path" location="${cache}/nexus-maven-repository-index" />
	<property name="mavenindex.path" location="${out}/mavenindex.sqlite3" />
	<property name="query-urilist" value="select path from (select * from pom union all select * from art) where packaging = 'jar' and ext = 'jar'" />
	<property name="query-bytecode" value="select pid, path from art where packaging = 'jar' and ext = 'jar'" />
	<property name="query-pom" value="select gid, aid, ver, path from pom where packaging = 'jar' and ext = 'jar'" />
	<property name="filter-dev" value=" and substr(gid, 1, 3) = 'ar.'" />
	<property name="filter-test" value=" and substr(gid, 1, 3) = 'es.'" />
	<property name="filter-last" value=" group by gid, aid having idate = max(idate)" />
	<property name="filter-prod" value="" />
	<property name="urilist.path" location="${out}/uri.list" />
	<property name="sessionlist.path" location="${out}/session.list" />
	<property name="repo" location="${cache}/repo" />
	<property name="mavenbytecode-dev.path" location="${out}/mavenbytecode-dev.sqlite3" />
	<property name="mavenbytecode-test.path" location="${out}/mavenbytecode-test.sqlite3" />
	<property name="mavenpom-dev.path" location="${out}/mavenpom-dev.sqlite3" />
	<property name="mavenpom-test.path" location="${out}/mavenpom-test.sqlite3" />
	<path id="lib">
		<fileset dir="${lib}">
			<include name="*.jar" />
		</fileset>
	</path>
	<target name="-mkbuilddir" description="Creates the *build* directory, used for output of compilation.">
		<mkdir dir="${build}" />
	</target>
	<target name="-mkcachedir" description="Creates the *cache* directory, used to cache downloaded files.">
		<mkdir dir="${cache}" />
	</target>
	<target name="-mkoutdir" description="Creates the *out* directory, used for output files.">
		<mkdir dir="${out}" />
	</target>
	<target name="-compile" depends="-mkbuilddir" description="Compiles all Java files.">
		<javac includeantruntime="false" srcdir="${src}" destdir="${build}">
			<classpath refid="lib" />
		</javac>
	</target>
	<target name="-fetchnexusindex" depends="-mkcachedir" description="Fetches the Nexus Maven Repository Index (GZipped) from the ibiblio mirror using *aria2*.">
		<exec executable="aria2c">
			<arg value="--dir=/" />
			<arg value="--max-concurrent-downloads=16" />
			<arg value="--auto-file-renaming=false" />
			<arg value="--conditional-get=true" />
			<arg value="--file-allocation=falloc" />
			<arg value="--out=${nexusindexgz.path}" />
			<arg value="${nexusindexgz.url}" />
		</exec>
	</target>
	<target name="nexusindex" depends="-fetchnexusindex" description="Fetches and uncompresses the Nexus Maven Repository Index from a mirror using *aria2*.">
		<gunzip src="${nexusindexgz.path}" dest="${nexusindex.path}" />
	</target>
	<target name="mavenindex" depends="-compile,-mkoutdir" description="Builds the Maven Index DB from the Nexus Index.">
		<delete file="${mavenindex.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenIndex" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--nexusindex=${nexusindex.path}" />
			<arg value="--mavenindex=${mavenindex.path}" />
		</java>
	</target>
	<target name="urilist-dev" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download in development configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-urilist} ${filter-dev}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="urilist-test" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download in test configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-urilist} ${filter-test}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="urilist-last" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download in last configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-urilist} ${filter-last}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="urilist-prod" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download in production configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-urilist} ${filter-prod}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="arts" description="Fetches all artifacts specified by the URI list in with *aria2*.">
		<exec executable="aria2c">
			<arg value="--dir=${repo}" />
			<arg value="--max-concurrent-downloads=16" />
			<arg value="--auto-file-renaming=false" />
			<arg value="--conditional-get=true" />
			<arg value="--input-file=${urilist.path}" />
			<arg value="--save-session=${sessionlist.path}" />
			<arg value="--deferred-input=true" />
		</exec>
	</target>
	<target name="mavenbytecode-dev" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${mavenbytecode-dev.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenBytecode" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-bytecode} ${filter-dev}" />
			<arg value="--mavenbytecode=${mavenbytecode-dev.path}" />
		</java>
	</target>
	<target name="mavenbytecode-test" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${mavenbytecode-test.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenBytecode" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-bytecode} ${filter-test}" />
			<arg value="--mavenbytecode=${mavenbytecode-test.path}" />
		</java>
	</target>
	<target name="mavenpom-dev" depends="-compile" description="Extract the information from POM files.">
		<delete file="${mavenpom-dev.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenPom" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-pom} ${filter-dev}" />
			<arg value="--mavenpom=${mavenpom-dev.path}" />
		</java>
	</target>
	<target name="mavenpom-test" depends="-compile" description="Extract the information from POM files.">
		<delete file="${mavenpom-test.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenPom" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-pom} ${filter-test}" />
			<arg value="--mavenpom=${mavenpom-test.path}" />
		</java>
	</target>
	<target name="mavends-dev" description="mavends db.">
		<delete file="out/mavends.sqlite3" />
		<exec executable="bash">
			<arg value="-c" />
			<arg value="cd ${out} &amp;&amp; cat ../resources/mavends.sql | sqlite3 -bail -echo mavends.sqlite3" />
		</exec>
	</target>
	<target name="clean" description="Removes the *build* and *out* directories.">
		<delete dir="${build}" />
		<delete dir="${out}" />
	</target>
</project>
