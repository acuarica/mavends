<project name="MavenDS" basedir="." default="-compile">
	<description>MavenDS: A Dataset and Analysis Framework for the Maven Repository. Targets begining with '.' are executed internally by other targets. To fetch the Nexus Index and Maven Artifact *aria2* (http://aria2.sourceforge.net/) is used.</description>
	<property name="src" location="src" />
	<property name="sql" location="sql" />
	<property name="lib" location="lib" />
	<property name="build" location="build" />
	<property name="cache" location="cache" />
	<property name="out" location="out" />
	<property name="mirror.1" value="http://mirrors.ibiblio.org/maven2" />
	<property name="mirror.2" value="http://maven.antelink.com/content/repositories/central" />
	<property name="mirror.3" value="http://repo.jfrog.org/artifactory/simple/libs-release-bintray" />
	<property name="nexusindexgz.url" value="${mirror.1}/.index/nexus-maven-repository-index.gz" />
	<property name="nexusindexgz.path" location="${cache}/nexus-maven-repository-index.gz" />
	<property name="nexusindex.path" location="${cache}/nexus-maven-repository-index" />
	<property name="mavenindex.path" location="${out}/mavenindex.sqlite3" />
	<property name="pomurilist.path" location="${out}/pomuri.list" />
	<property name="pomsessionlist.path" location="${out}/pomsession.list" />
	<property name="repo" location="${cache}/repo" />
	<macrodef name="set">
		<attribute name="name" />
		<attribute name="value" />
		<sequential>
			<echo message="@{name}=@{value}" />
			<property name="@{name}" value="@{value}" />
		</sequential>
	</macrodef>
	<macrodef name="setup">
		<attribute name="conf" />
		<attribute name="where" />
		<sequential>
			<set name="select.query" value="select coorid, groupid, artifactid, version, classifier, extension, path from artifact_jar where @{where}" />
			<set name="select.path" value="${out}/selectarts-@{conf}.csv" />
			<set name="urilist.path" value="${out}/uri-@{conf}.list" />
			<set name="sessionlist.path" value="${out}/session-@{conf}.list" />
			<set name="maveninode.path" value="${out}/maveninode-@{conf}.sqlite3" />
			<set name="mavenpom.path" value="${out}/mavenpom-@{conf}.sqlite3" />
		</sequential>
	</macrodef>
	<target name="dev">
		<setup conf="dev" where="rootgroup = 'es'" />
	</target>
	<target name="test">
		<setup conf="test" where="rootgroup = 'net'" />
	</target>
	<target name="stage">
		<setup conf="stage" where="rootgroup = 'com'" />
	</target>
	<target name="prod">
		<setup conf="prod" where="1" />
	</target>
	<path id="lib">
		<fileset dir="${lib}">
			<include name="*.jar" />
		</fileset>
	</path>
	<target name="-mkbuilddir" description="Creates the build/ directory, used for output of compilation.">
		<mkdir dir="${build}" />
	</target>
	<target name="-mkcachedir" description="Creates the cache/ directory, used to cache downloaded files.">
		<mkdir dir="${cache}" />
	</target>
	<target name="-mkoutdir" description="Creates the out/ directory, used for output files.">
		<mkdir dir="${out}" />
	</target>
	<target name="-compile" depends="-mkbuilddir" description="Compiles all Java files under the src/ directory.">
		<javac includeantruntime="false" srcdir="${src}" destdir="${build}" debug="true">
			<classpath refid="lib" />
		</javac>
	</target>
	<target name="nexusindex" depends="-mkcachedir" description="Fetches and uncompresses the Nexus Maven Repository Index.">
		<exec executable="aria2c">
			<arg value="--dir=/" />
			<arg value="--max-concurrent-downloads=16" />
			<arg value="--auto-file-renaming=false" />
			<arg value="--conditional-get=true" />
			<arg value="--allow-overwrite=true" />
			<arg value="--file-allocation=falloc" />
			<arg value="--out=${nexusindexgz.path}" />
			<arg value="${nexusindexgz.url}" />
		</exec>
		<gunzip src="${nexusindexgz.path}" dest="${nexusindex.path}" />
	</target>
	<target name="mavenindex" depends="-compile,-mkoutdir" description="Creates and populates the Maven Index DB from the Nexus Index.">
		<java classname="ch.usi.inf.mavends.ExportNexusIndex" classpath="${build}">
			<classpath refid="lib" />
			<arg value="--nexusindex=${nexusindex.path}" />
			<arg value="--out=${out}" />
		</java>
		<delete file="${mavenindex.path}" />
		<exec executable="sqlite3">
			<arg value="-bail" />
			<arg value="-cmd" />
			<arg value=".read ${sql}/mavenindex.sql" />
			<arg value="-cmd" />
			<arg value=".import ${out}/nexus-header.csv header" />
			<arg value="-cmd" />
			<arg value=".import ${out}/nexus-us.csv artifact" />
			<arg value="-cmd" />
			<arg value=".import ${out}/nexus-dels.csv del" />
			<arg value="-cmd" />
			<arg value=".import ${out}/nexus-desc.csv descriptor" />
			<arg value="-cmd" />
			<arg value=".import ${out}/nexus-all.csv allgroups" />
			<arg value="-cmd" />
			<arg value=".import ${out}/nexus-root.csv rootgroups" />
			<arg value="-cmd" />
			<arg value="update artifact set classifier = null where classifier = ''" />
			<arg value="-cmd" />
			<arg value="update del set classifier = null where classifier = ''" />
			<arg value="${mavenindex.path}" />
		</exec>
	</target>
	<target name="selectarts">
		<exec executable="sqlite3">
			<arg value="-bail" />
			<arg value="-cmd" />
			<arg value=".output ${select.path}" />
			<arg value="-cmd" />
			<arg value="${select.query}" />
			<arg value="${mavenindex.path}" />
		</exec>
	</target>
	<target name="urilist" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}" input="${select.path}">
			<classpath refid="lib" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="fetcharts" description="Fetches all artifacts specified by the URI list in with *aria2*.">
		<exec executable="aria2c">
			<arg value="--dir=${repo}" />
			<arg value="--max-concurrent-downloads=16" />
			<arg value="--auto-file-renaming=false" />
			<arg value="--conditional-get=true" />
			<arg value="--allow-overwrite=true" />
			<arg value="--input-file=${urilist.path}" />
			<arg value="--save-session=${sessionlist.path}" />
			<arg value="--deferred-input=true" />
		</exec>
	</target>
	<target name="maveninode" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${maveninode.path}" />
		<exec executable="sqlite3" input="${sql}/maveninode.sql">
			<arg value="-bail" />
			<arg value="-echo" />
			<arg value="${maveninode.path}" />
		</exec>
		<java classname="ch.usi.inf.mavends.BuildMavenInode" classpath="${build}:${resources}" input="out/list.es.csv">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${select.query}" />
			<arg value="--maveninode=${maveninode.path}" />
		</java>
	</target>
	<target name="mavenpom" depends="-compile" description="Extract the information from POM files.">
		<delete file="${mavenpom-dev.path}" />
		<exec executable="sqlite3" input="${sql}/mavenpom.sql">
			<arg value="-bail" />
			<arg value="-echo" />
			<arg value="${mavenpom-dev.path}" />
		</exec>
		<java classname="ch.usi.inf.mavends.BuildMavenPom" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-dev}" />
			<arg value="--mavenpom=${mavenpom-dev.path}" />
		</java>
	</target>
	<target name="clean" description="Removes the compilation directory, e.g., *build*.">
		<delete dir="${build}" />
	</target>
</project>
