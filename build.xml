<project name="MavenDS" basedir=".">
	<description>MavenDS: A Dataset and Analysis Framework for the Maven Repository. Targets begining with '-' are executed internally by other targets.</description>
	<property name="src" location="src" />
	<property name="resources" location="resources" />
	<property name="lib" location="lib" />
	<property name="build" location="build" />
	<property name="cache" location="cache" />
	<property name="out" location="out" />
	<property name="mirror.1" value="http://mirrors.ibiblio.org/maven2" />
	<property name="mirror.2" value="http://maven.antelink.com/content/repositories/central" />
	<property name="mirror.3" value="http://repo.jfrog.org/artifactory/simple/libs-release-bintray" />
	<property name="nexusindexgz.url" value="${mirror.1}/.index/nexus-maven-repository-index.gz" />
	<property name="nexusindexgz.path" location="${cache}/nexus-maven-repository-index.gz" />
	<property name="nexusindex.path" location="${cache}/nexus-maven-repository-index" />
	<property name="mavenindex.path" location="${out}/mavenindex.sqlite3" />
	<property name="query-dev" value="select coorid from artifact_jar where rootgroup = 'ar'" />
	<property name="query-test" value="select coorid from artifact_jar where rootgroup = 'es'" />
	<property name="query-stage" value="select coorid from artifact_jar where rootgroup = 'net'" />
	<property name="query-last" value="select coorid from artifact_jar group by groupid, artifactid having idate = max(idate)" />
	<property name="query-prod" value="select coorid from artifact_jar" />
	<property name="urilist.path" location="${out}/uri.list" />
	<property name="sessionlist.path" location="${out}/session.list" />
	<property name="repo" location="${cache}/repo" />
	<property name="mavenbytecode-dev.path" location="${out}/mavenbytecode-dev.sqlite3" />
	<property name="mavenbytecode-test.path" location="${out}/mavenbytecode-test.sqlite3" />
	<property name="mavenbytecode-stage.path" location="${out}/mavenbytecode-stage.sqlite3" />
	<property name="mavenbytecode-prod.path" location="${out}/mavenbytecode-prod.sqlite3" />
	<property name="mavenpom-dev.path" location="${out}/mavenpom-dev.sqlite3" />
	<property name="mavenpom-test.path" location="${out}/mavenpom-test.sqlite3" />
	<path id="lib">
		<fileset dir="${lib}">
			<include name="*.jar" />
		</fileset>
	</path>
	<target name="-mkbuilddir" description="Creates the build/ directory, used for output of compilation.">
		<mkdir dir="${build}" />
	</target>
	<target name="-mkcachedir" description="Creates the cache/ directory, used to cache downloaded files.">
		<mkdir dir="${cache}" />
	</target>
	<target name="-mkoutdir" description="Creates the out/ directory, used for output files.">
		<mkdir dir="${out}" />
	</target>
	<target name="-compile" depends="-mkbuilddir" description="Compiles all Java files under the src/ directory.">
		<javac includeantruntime="false" srcdir="${src}" destdir="${build}">
			<classpath refid="lib" />
		</javac>
	</target>
	<target name="fetchnexusindex" depends="-mkcachedir" description="Fetches the Nexus Maven Repository Index (GZipped) from ibiblio.org mirror using *aria2* (http://aria2.sourceforge.net/).">
		<exec executable="aria2c">
			<arg value="--dir=/" />
			<arg value="--max-concurrent-downloads=16" />
			<arg value="--auto-file-renaming=false" />
			<arg value="--conditional-get=true" />
			<arg value="--allow-overwrite=true" />
			<arg value="--file-allocation=falloc" />
			<arg value="--out=${nexusindexgz.path}" />
			<arg value="${nexusindexgz.url}" />
		</exec>
	</target>
	<target name="nexusindex" depends="fetchnexusindex" description="Fetches and uncompresses the Nexus Maven Repository Index from ibiblio.org mirror using *aria2*. See target fetchnexusindex.">
		<gunzip src="${nexusindexgz.path}" dest="${nexusindex.path}" />
	</target>
	<target name="checkmavenindex" depends="-compile,-mkoutdir" description="Checks if the Nexus Index is well formed.">
		<delete file="${mavenindex.path}" />
		<java classname="ch.usi.inf.mavends.CheckMavenIndex" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--nexusindex=${nexusindex.path}" />
		</java>
	</target>
	<target name="mavenindex" depends="-compile,-mkoutdir" description="Builds the Maven Index DB from the Nexus Index.">
		<delete file="${mavenindex.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenIndex" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--nexusindex=${nexusindex.path}" />
			<arg value="--mavenindex=${mavenindex.path}" />
		</java>
	</target>
	<target name="mavenindex-views" description="Creates views and materialized views (tables in SQLite) in the Maven Index DB.">
		<exec executable="sqlite3" input="resources/mavenindex-views.sql">
			<arg value="-bail" />
			<arg value="-echo" />
			<arg value="${mavenindex.path}" />
		</exec>
	</target>
	<target name="urilist-dev" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-dev}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="urilist-test" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-test}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="urilist-stage" depends="-compile,-mkoutdir" description="Builds the list of artifacts to download configuration.">
		<java classname="ch.usi.inf.mavends.BuildUriList" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--urilist=${urilist.path}" />
			<arg value="--query=${query-stage}" />
			<arg value="--mirrors=${mirror.1},${mirror.2},${mirror.3}" />
		</java>
	</target>
	<target name="fetcharts" description="Fetches all artifacts specified by the URI list in with *aria2*.">
		<exec executable="aria2c">
			<arg value="--dir=${repo}" />
			<arg value="--max-concurrent-downloads=16" />
			<arg value="--auto-file-renaming=false" />
			<arg value="--conditional-get=true" />
			<arg value="--allow-overwrite=true" />
			<arg value="--input-file=${urilist.path}" />
			<arg value="--save-session=${sessionlist.path}" />
			<arg value="--deferred-input=true" />
		</exec>
	</target>
	<target name="mavenbytecode-dev" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${mavenbytecode-dev.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenBytecode" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-dev}" />
			<arg value="--mavenbytecode=${mavenbytecode-dev.path}" />
		</java>
	</target>
	<target name="mavenbytecode-test" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${mavenbytecode-test.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenBytecode" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-test}" />
			<arg value="--mavenbytecode=${mavenbytecode-test.path}" />
		</java>
	</target>
	<target name="mavenbytecode-stage" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${mavenbytecode-stage.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenBytecode" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-stage}" />
			<arg value="--mavenbytecode=${mavenbytecode-stage.path}" />
		</java>
	</target>
	<target name="mavenbytecode-prod" depends="-compile" description="Extract the dependency information from POM files.">
		<delete file="${mavenbytecode-prod.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenBytecode" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-prod}" />
			<arg value="--mavenbytecode=${mavenbytecode-prod.path}" />
		</java>
	</target>
	<target name="mavenpom-dev" depends="-compile" description="Extract the information from POM files.">
		<delete file="${mavenpom-dev.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenPom" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-test}" />
			<arg value="--mavenpom=${mavenpom-dev.path}" />
		</java>
	</target>
	<target name="mavenpom-test" depends="-compile" description="Extract the information from POM files.">
		<delete file="${mavenpom-test.path}" />
		<java classname="ch.usi.inf.mavends.BuildMavenPom" classpath="${build}:${resources}">
			<classpath refid="lib" />
			<arg value="--mavenindex=${mavenindex.path}" />
			<arg value="--repo=${repo}" />
			<arg value="--query=${query-test}" />
			<arg value="--mavenpom=${mavenpom-test.path}" />
		</java>
	</target>
	<target name="clean" description="Removes the *build* directory.">
		<delete dir="${build}" />
	</target>
</project>
